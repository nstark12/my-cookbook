<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {{#if title}}
        <title>{{title}} | myCookbook</title>
    {{else}}
        <title>myCookbook</title>
    {{/if}}
    <!-- Custom Style Sheet -->
    <link rel="stylesheet" href="/css/style.css">
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;1,300;1,400&display=swap" rel="stylesheet">
</head>
<body>
    <header>
        <div class="logo">
            <a href="/"><img src="/assets/images/logo-2.png" alt="mycookbook logo" class="logo-img"></a>
        </div>

        <div class="navbar">
            <ul>
                <a href="#"><li>Shopping Cart</li></a>
                <a href="#"><li>Recipe Book</li></a>
                {{> log-in-out}}
            </ul>
        </div>
    </header>

    <main>
        {{{ body }}}
    </main>
    
<script src="/js/logout.js"></script>
</body>
</html>